# .env should NOT be checked in to source control
SECRET_TOKEN=secret # replace with `rake secret`
GOOGLE_ANALYTICS=UA-12345678-9 # replace with your Google Analytics id
DATABASE_PASSWORD=secret # replace with your database password. Perhaps `SecureRandom.urlsafe_base64(24)`
